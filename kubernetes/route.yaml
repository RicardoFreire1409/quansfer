apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: backend
  labels: { app: backend, env: dev }
spec:
  to: { kind: Service, name: backend-svc }
  port: { targetPort: http }
  tls: { termination: edge }
